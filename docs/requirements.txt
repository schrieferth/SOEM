# Documentation build dependencies for this SOEM fork (docs/).
# Mirrors the Testknecht docs toolchain (Sphinx + MyST Markdown).
#
#   python3 -m venv docs/.venv && docs/.venv/bin/pip install -r docs/requirements.txt
#   cd docs && make html
sphinx>=7
myst-parser>=2
sphinx-rtd-theme>=2
sphinx-copybutton>=0.5
sphinxcontrib-mermaid>=0.9
